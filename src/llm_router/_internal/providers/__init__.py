"""Provider adapter package.

Why:
    Owns concrete SDK adapters and provider-neutral adapter ports.

What belongs here:
    Adapter protocols, provider registries, retry helpers, and concrete
    provider modules.

What does not belong here:
    Public DTO declarations, session persistence, or route-policy state.
"""
