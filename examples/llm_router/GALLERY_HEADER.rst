Examples
========

These are complete live programs built only on the public ``llm_router`` API.
They use real providers, credentials, and external services when executed.
Required CI imports and type-checks them but does not run their live workflows.
