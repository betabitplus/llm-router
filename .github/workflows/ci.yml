name: CI

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, edited]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  ci:
    name: ci
    uses: betabitplus/ternforge-infra-ci/.github/workflows/python-library.yml@65e75e159bb39fd569f662b97ccae950e2365df0 # v5.8.3
    with:
      python-version: "3.13"
      runtime-audit-exclude-package: "py-lib-runtime"
      runtime-audit-ignore-vulnerabilities: ""
      playwright-browsers: ""
    secrets:
      app-private-key: ${{ secrets.TERNFORGE_SOURCE_READ_PRIVATE_KEY }}
